services:
  app:
    build: .
    ports:
      - "8000:7860"
    env_file:
      - .env
    volumes:
      # Persist session history across container restarts
      - ./data/history.db:/app/data/history.db
    restart: unless-stopped
